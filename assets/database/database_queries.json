{
    "createJournalIfNotExists": "CREATE TABLE IF NOT EXISTS journalEntries(id INTEGER PRIMARY KEY AUTOINCREMENT, title TEXT NOT NULL, body TEXT NOT NULL, rating INTEGER NOT NULL, date TEXT NOT NULL);",
    "selectAllJournalEntries": "SELECT * FROM journalEntries;",
    "insertJournalEntry": "INSERT INTO journalEntries (title, body, rating, date) VALUES (?, ?, ?, ?);"
}
